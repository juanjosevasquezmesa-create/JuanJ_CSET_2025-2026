const prompt = require("prompt-sync")();

let first_num = prompt("please enter a number: ");

let second_num = prompt("A second number, please: ");

console.log(`The sum is, ${first_num + second_num}`);
//this will combine the numbers like strings