function name(para1, para2)//because javascript have free type variables you just create names and they will be the variables inside of the code 
{
    console.log("Hello, " + para2 + ", " + para1);
}
name("juan", "vasquez");